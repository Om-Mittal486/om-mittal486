name: GitHub Snake Game

on:
  schedule:
    - cron: "0 0 * * *"  # Runs every day at midnight
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Generate GitHub Contributions Snake Animations
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=#008CFF&color_dots=#8be9fd,#6272a4,#bd93f9,#ff79c6,#50fa7b
            dist/github-snake-dark.svg?palette=github-dark&color_snake=#008CFF&color_dots=#8be9fd,#6272a4,#bd93f9,#ff79c6,#50fa7b
            dist/ocean.gif?color_snake=#008CFF&color_dots=#8be9fd,#6272a4,#bd93f9,#ff79c6,#50fa7b
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy to Output Branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: output
          commit_message: "Update snake animation with Cyberpunk colors [skip ci]"
